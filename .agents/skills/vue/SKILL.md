---
name: "vue"
description: "Vue 3 (via `vike-vue`) — the UI framework this app renders with. Use when writing Single-File Components, using the Composition API (`ref`/`reactive`/`computed`/`watch`), lifecycle, or fixing SSR/hydration mismatches."
---

Up-to-date Vue documentation for this project. Read it before working with Vue:

https://vuejs.org/llms.txt
